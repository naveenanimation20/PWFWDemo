# ============================================
# OPTION 1: FULLY AUTOMATED PIPELINE
# ============================================
# Flow: dev → qa → stage → prod (automatic)
# Trigger: Push to develop/main branches
# Reports: Published to GitHub Pages
# ✅ Playwright browsers cached for faster runs
# ✅ Allure environment labels for separation
# ============================================

name: Playwright Auto Pipeline

on:
  push:
    branches: [main, master, develop]
  pull_request:
    branches: [main, master]

env:
  NODE_VERSION: '20'

jobs:
  # ============================================
  # DEV Environment Tests
  # ============================================
  dev-tests:
    name: 🔧 DEV Tests
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: 📚 Install dependencies
        run: npm ci

      # ✅ Cache Playwright browsers
      - name: 🗄️ Cache Playwright browsers
        uses: actions/cache@v4
        id: playwright-cache
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            playwright-${{ runner.os }}-

      - name: 🎭 Install Playwright browsers
        if: steps.playwright-cache.outputs.cache-hit != 'true'
        run: npx playwright install --with-deps

      - name: 🎭 Install Playwright dependencies (if cached)
        if: steps.playwright-cache.outputs.cache-hit == 'true'
        run: npx playwright install-deps

      # ✅ Clean old results
      - name: 🧹 Clean previous results
        run: |
          rm -rf allure-results playwright-report playwright-html-report test-results

      - name: 🧪 Run DEV tests
        run: npx playwright test --grep "@newone" --config=playwright.config.dev.ts
        env:
          CI: true
          ALLURE_ENVIRONMENT: DEV

      # ✅ Add environment label to Allure results
      - name: 🏷️ Add Allure environment info
        if: always()
        run: |
          echo "Environment=DEV" > allure-results/environment.properties
          echo "Browser=Google Chrome" >> allure-results/environment.properties
          echo "Config=playwright.config.dev.ts" >> allure-results/environment.properties
          # Add parent suite label to each result file
          for file in allure-results/*-result.json; do
            if [ -f "$file" ]; then
              # Add DEV label to test names
              sed -i 's/"name":"\([^"]*\)"/"name":"[DEV] \1"/g' "$file" 2>/dev/null || true
            fi
          done

      - name: 📊 Upload HTML Report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-report-dev
          path: playwright-report/
          retention-days: 30

      - name: 📊 Upload Custom HTML Report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-html-report-dev
          path: playwright-html-report/
          retention-days: 30

      - name: 📊 Upload Allure Results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: allure-results-dev
          path: allure-results/
          retention-days: 30

      - name: 📸 Upload Test Results (Screenshots/Videos)
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: test-results-dev
          path: test-results/
          retention-days: 7

  # ============================================
  # QA Environment Tests
  # ============================================
  qa-tests:
    name: 🔍 QA Tests
    runs-on: ubuntu-latest
    needs: dev-tests
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: 📚 Install dependencies
        run: npm ci

      # ✅ Cache Playwright browsers
      - name: 🗄️ Cache Playwright browsers
        uses: actions/cache@v4
        id: playwright-cache
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            playwright-${{ runner.os }}-

      - name: 🎭 Install Playwright browsers
        if: steps.playwright-cache.outputs.cache-hit != 'true'
        run: npx playwright install --with-deps

      - name: 🎭 Install Playwright dependencies (if cached)
        if: steps.playwright-cache.outputs.cache-hit == 'true'
        run: npx playwright install-deps

      # ✅ Clean old results
      - name: 🧹 Clean previous results
        run: |
          rm -rf allure-results playwright-report playwright-html-report test-results

      - name: 🧪 Run QA tests
        run: npx playwright test --grep "@newone" --config=playwright.config.qa.ts
        env:
          CI: true
          ALLURE_ENVIRONMENT: QA

      # ✅ Add environment label to Allure results
      - name: 🏷️ Add Allure environment info
        if: always()
        run: |
          echo "Environment=QA" > allure-results/environment.properties
          echo "Browser=Google Chrome" >> allure-results/environment.properties
          echo "Config=playwright.config.qa.ts" >> allure-results/environment.properties
          # Add parent suite label to each result file
          for file in allure-results/*-result.json; do
            if [ -f "$file" ]; then
              sed -i 's/"name":"\([^"]*\)"/"name":"[QA] \1"/g' "$file" 2>/dev/null || true
            fi
          done

      - name: 📊 Upload HTML Report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-report-qa
          path: playwright-report/
          retention-days: 30

      - name: 📊 Upload Custom HTML Report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-html-report-qa
          path: playwright-html-report/
          retention-days: 30

      - name: 📊 Upload Allure Results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: allure-results-qa
          path: allure-results/
          retention-days: 30

      - name: 📸 Upload Test Results (Screenshots/Videos)
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: test-results-qa
          path: test-results/
          retention-days: 7

  # ============================================
  # STAGE Environment Tests
  # ============================================
  stage-tests:
    name: 🎯 STAGE Tests
    runs-on: ubuntu-latest
    needs: qa-tests
    environment:
      name: staging
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: 📚 Install dependencies
        run: npm ci

      # ✅ Cache Playwright browsers
      - name: 🗄️ Cache Playwright browsers
        uses: actions/cache@v4
        id: playwright-cache
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            playwright-${{ runner.os }}-

      - name: 🎭 Install Playwright browsers
        if: steps.playwright-cache.outputs.cache-hit != 'true'
        run: npx playwright install --with-deps

      - name: 🎭 Install Playwright dependencies (if cached)
        if: steps.playwright-cache.outputs.cache-hit == 'true'
        run: npx playwright install-deps

      # ✅ Clean old results
      - name: 🧹 Clean previous results
        run: |
          rm -rf allure-results playwright-report playwright-html-report test-results

      - name: 🧪 Run STAGE tests
        run: npx playwright test --grep "@newone" --config=playwright.config.stage.ts
        env:
          CI: true
          ALLURE_ENVIRONMENT: STAGE

      # ✅ Add environment label to Allure results
      - name: 🏷️ Add Allure environment info
        if: always()
        run: |
          echo "Environment=STAGE" > allure-results/environment.properties
          echo "Browser=Google Chrome" >> allure-results/environment.properties
          echo "Config=playwright.config.stage.ts" >> allure-results/environment.properties
          # Add parent suite label to each result file
          for file in allure-results/*-result.json; do
            if [ -f "$file" ]; then
              sed -i 's/"name":"\([^"]*\)"/"name":"[STAGE] \1"/g' "$file" 2>/dev/null || true
            fi
          done

      - name: 📊 Upload HTML Report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-report-stage
          path: playwright-report/
          retention-days: 30

      - name: 📊 Upload Custom HTML Report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-html-report-stage
          path: playwright-html-report/
          retention-days: 30

      - name: 📊 Upload Allure Results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: allure-results-stage
          path: allure-results/
          retention-days: 30

      - name: 📸 Upload Test Results (Screenshots/Videos)
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: test-results-stage
          path: test-results/
          retention-days: 7

  # ============================================
  # PROD Environment Tests
  # ============================================
  prod-tests:
    name: 🚀 PROD Tests
    runs-on: ubuntu-latest
    needs: stage-tests
    environment:
      name: production
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: 📚 Install dependencies
        run: npm ci

      # ✅ Cache Playwright browsers
      - name: 🗄️ Cache Playwright browsers
        uses: actions/cache@v4
        id: playwright-cache
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            playwright-${{ runner.os }}-

      - name: 🎭 Install Playwright browsers
        if: steps.playwright-cache.outputs.cache-hit != 'true'
        run: npx playwright install --with-deps

      - name: 🎭 Install Playwright dependencies (if cached)
        if: steps.playwright-cache.outputs.cache-hit == 'true'
        run: npx playwright install-deps

      # ✅ Clean old results
      - name: 🧹 Clean previous results
        run: |
          rm -rf allure-results playwright-report playwright-html-report test-results

      - name: 🧪 Run PROD tests
        run: npx playwright test --grep "@newone" --config=playwright.config.prod.ts
        env:
          CI: true
          ALLURE_ENVIRONMENT: PROD

      # ✅ Add environment label to Allure results
      - name: 🏷️ Add Allure environment info
        if: always()
        run: |
          echo "Environment=PROD" > allure-results/environment.properties
          echo "Browser=Google Chrome" >> allure-results/environment.properties
          echo "Config=playwright.config.prod.ts" >> allure-results/environment.properties
          # Add parent suite label to each result file
          for file in allure-results/*-result.json; do
            if [ -f "$file" ]; then
              sed -i 's/"name":"\([^"]*\)"/"name":"[PROD] \1"/g' "$file" 2>/dev/null || true
            fi
          done

      - name: 📊 Upload HTML Report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-report-prod
          path: playwright-report/
          retention-days: 30

      - name: 📊 Upload Custom HTML Report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-html-report-prod
          path: playwright-html-report/
          retention-days: 30

      - name: 📊 Upload Allure Results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: allure-results-prod
          path: allure-results/
          retention-days: 30

      - name: 📸 Upload Test Results (Screenshots/Videos)
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: test-results-prod
          path: test-results/
          retention-days: 7

  # ============================================
  # Publish All Reports to GitHub Pages
  # ============================================
  publish-reports:
    name: 📈 Publish Reports
    runs-on: ubuntu-latest
    needs: [dev-tests, qa-tests, stage-tests, prod-tests]
    if: always()
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      # ========== Download All Artifacts ==========
      - name: 📥 Download DEV Playwright Report
        uses: actions/download-artifact@v4
        with:
          name: playwright-report-dev
          path: github-pages/dev/playwright-report
        continue-on-error: true

      - name: 📥 Download DEV HTML Report
        uses: actions/download-artifact@v4
        with:
          name: playwright-html-report-dev
          path: github-pages/dev/playwright-html-report
        continue-on-error: true

      - name: 📥 Download QA Playwright Report
        uses: actions/download-artifact@v4
        with:
          name: playwright-report-qa
          path: github-pages/qa/playwright-report
        continue-on-error: true

      - name: 📥 Download QA HTML Report
        uses: actions/download-artifact@v4
        with:
          name: playwright-html-report-qa
          path: github-pages/qa/playwright-html-report
        continue-on-error: true

      - name: 📥 Download STAGE Playwright Report
        uses: actions/download-artifact@v4
        with:
          name: playwright-report-stage
          path: github-pages/stage/playwright-report
        continue-on-error: true

      - name: 📥 Download STAGE HTML Report
        uses: actions/download-artifact@v4
        with:
          name: playwright-html-report-stage
          path: github-pages/stage/playwright-html-report
        continue-on-error: true

      - name: 📥 Download PROD Playwright Report
        uses: actions/download-artifact@v4
        with:
          name: playwright-report-prod
          path: github-pages/prod/playwright-report
        continue-on-error: true

      - name: 📥 Download PROD HTML Report
        uses: actions/download-artifact@v4
        with:
          name: playwright-html-report-prod
          path: github-pages/prod/playwright-html-report
        continue-on-error: true

      # ========== Allure Reports ==========
      - name: 📥 Download DEV Allure Results
        uses: actions/download-artifact@v4
        with:
          name: allure-results-dev
          path: allure-results/dev
        continue-on-error: true

      - name: 📥 Download QA Allure Results
        uses: actions/download-artifact@v4
        with:
          name: allure-results-qa
          path: allure-results/qa
        continue-on-error: true

      - name: 📥 Download STAGE Allure Results
        uses: actions/download-artifact@v4
        with:
          name: allure-results-stage
          path: allure-results/stage
        continue-on-error: true

      - name: 📥 Download PROD Allure Results
        uses: actions/download-artifact@v4
        with:
          name: allure-results-prod
          path: allure-results/prod
        continue-on-error: true

      - name: 🔀 Merge All Allure Results
        run: |
          mkdir -p allure-results-merged
          
          echo "📂 Checking downloaded artifacts..."
          ls -la allure-results/
          
          for env in dev qa stage prod; do
            echo "Processing $env..."
            if [ -d "allure-results/$env" ]; then
              echo "  Found allure-results/$env"
              ls -la allure-results/$env/ | head -5
              
              for file in allure-results/$env/*-result.json; do
                if [ -f "$file" ]; then
                  filename=$(basename "$file")
                  newfile="allure-results-merged/${env}_${filename}"
                  
                  # Modify historyId and uuid to make unique per environment
                  sed -e "s/\"historyId\":\"/\"historyId\":\"${env}_/g" \
                      -e "s/\"uuid\":\"/\"uuid\":\"${env}_/g" \
                      -e "s/\"name\":\"/\"name\":\"[${env^^}] /g" \
                      "$file" > "$newfile"
                  
                  echo "  ✅ Copied: $filename -> ${env}_${filename}"
                fi
              done
              
              # Copy container files
              for file in allure-results/$env/*-container.json; do
                if [ -f "$file" ]; then
                  filename=$(basename "$file")
                  sed "s/\"uuid\":\"/\"uuid\":\"${env}_/g" "$file" > "allure-results-merged/${env}_${filename}"
                fi
              done
              
              # Copy attachment files
              for file in allure-results/$env/*-attachment.*; do
                if [ -f "$file" ]; then
                  filename=$(basename "$file")
                  cp "$file" "allure-results-merged/${env}_${filename}"
                fi
              done
              
              # Copy environment.properties
              if [ -f "allure-results/$env/environment.properties" ]; then
                cat "allure-results/$env/environment.properties" >> "allure-results-merged/environment.properties"
                echo "" >> "allure-results-merged/environment.properties"
              fi
            else
              echo "  ⚠️ Directory allure-results/$env not found"
            fi
          done
          
          echo ""
          echo "📊 Merged Allure Results:"
          ls -la allure-results-merged/ | head -30
          echo "Total files: $(ls allure-results-merged/ | wc -l)"

      - name: 📦 Setup Java (for Allure)
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: 📊 Install Allure CLI
        run: |
          curl -o allure-2.24.0.tgz -Ls https://repo.maven.apache.org/maven2/io/qameta/allure/allure-commandline/2.24.0/allure-commandline-2.24.0.tgz
          tar -zxvf allure-2.24.0.tgz
          echo "$PWD/allure-2.24.0/bin" >> $GITHUB_PATH

      - name: 📊 Generate Allure Report
        run: |
          allure generate allure-results-merged -o github-pages/allure-report --clean
        continue-on-error: true

      # ========== Create Index Page ==========
      - name: 📝 Create Index Page
        run: |
          CURRENT_DATE=$(date '+%Y-%m-%d %H:%M:%S UTC')
          cat > github-pages/index.html << EOF
          <!DOCTYPE html>
          <html>
          <head>
            <title>Playwright Test Reports - PWFWDemo</title>
            <style>
              body { font-family: Arial, sans-serif; max-width: 900px; margin: 50px auto; padding: 20px; }
              h1 { color: #2c3e50; border-bottom: 3px solid #3498db; padding-bottom: 10px; }
              h2 { color: #34495e; margin-top: 30px; }
              .env-section { background: #f8f9fa; padding: 20px; border-radius: 8px; margin: 15px 0; }
              .env-section h3 { margin-top: 0; }
              a { color: #3498db; text-decoration: none; display: inline-block; margin: 5px 10px 5px 0; padding: 8px 16px; background: #ecf0f1; border-radius: 4px; }
              a:hover { background: #3498db; color: white; }
              .dev { border-left: 4px solid #27ae60; }
              .qa { border-left: 4px solid #f39c12; }
              .stage { border-left: 4px solid #9b59b6; }
              .prod { border-left: 4px solid #e74c3c; }
              .allure { border-left: 4px solid #3498db; }
              .timestamp { color: #7f8c8d; font-size: 0.9em; }
            </style>
          </head>
          <body>
            <h1>🎭 Playwright Test Reports</h1>
            <p class="timestamp">Last updated: ${CURRENT_DATE}</p>
            
            <div class="env-section allure">
              <h3>📊 Allure Report (All Environments)</h3>
              <a href="./allure-report/index.html">View Allure Report</a>
            </div>

            <div class="env-section dev">
              <h3>🔧 DEV Environment</h3>
              <a href="./dev/playwright-report/index.html">Playwright Report</a>
              <a href="./dev/playwright-html-report/index.html">HTML Report</a>
            </div>
            
            <div class="env-section qa">
              <h3>🔍 QA Environment</h3>
              <a href="./qa/playwright-report/index.html">Playwright Report</a>
              <a href="./qa/playwright-html-report/index.html">HTML Report</a>
            </div>
            
            <div class="env-section stage">
              <h3>🎯 STAGE Environment</h3>
              <a href="./stage/playwright-report/index.html">Playwright Report</a>
              <a href="./stage/playwright-html-report/index.html">HTML Report</a>
            </div>
            
            <div class="env-section prod">
              <h3>🚀 PROD Environment</h3>
              <a href="./prod/playwright-report/index.html">Playwright Report</a>
              <a href="./prod/playwright-html-report/index.html">HTML Report</a>
            </div>
          </body>
          </html>
          EOF

      # ========== Deploy to GitHub Pages ==========
      - name: 🌐 Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_branch: gh-pages
          publish_dir: github-pages

      # ========== Print Report URLs ==========
      - name: 🔗 Report URLs
        run: |
          echo "============================================"
          echo "📊 REPORTS PUBLISHED SUCCESSFULLY!"
          echo "============================================"
          echo ""
          echo "🏠 Main Dashboard:"
          echo "   https://naveenanimation20.github.io/PWFWDemo/"
          echo ""
          echo "📈 Allure Report:"
          echo "   https://naveenanimation20.github.io/PWFWDemo/allure-report/index.html"
          echo ""
          echo "🔧 DEV Reports:"
          echo "   Playwright: https://naveenanimation20.github.io/PWFWDemo/dev/playwright-report/index.html"
          echo "   HTML:       https://naveenanimation20.github.io/PWFWDemo/dev/playwright-html-report/index.html"
          echo ""
          echo "🔍 QA Reports:"
          echo "   Playwright: https://naveenanimation20.github.io/PWFWDemo/qa/playwright-report/index.html"
          echo "   HTML:       https://naveenanimation20.github.io/PWFWDemo/qa/playwright-html-report/index.html"
          echo ""
          echo "🎯 STAGE Reports:"
          echo "   Playwright: https://naveenanimation20.github.io/PWFWDemo/stage/playwright-report/index.html"
          echo "   HTML:       https://naveenanimation20.github.io/PWFWDemo/stage/playwright-html-report/index.html"
          echo ""
          echo "🚀 PROD Reports:"
          echo "   Playwright: https://naveenanimation20.github.io/PWFWDemo/prod/playwright-report/index.html"
          echo "   HTML:       https://naveenanimation20.github.io/PWFWDemo/prod/playwright-html-report/index.html"
          echo ""
          echo "============================================"